syntax = "proto3";

package dart_test;

message LoginInfo {
    string email=1;
    string password=2;
}

message Result {
    int32 code=1;
    string msg=2; 
}

message UserProfile {
    string name=1;
    string gender=2;
    int32 age=3;
}

service Login {
    rpc Login(LoginInfo) returns(Result);
    rpc UpdateUserProfile(UserProfile) returns(Result); 
} 
